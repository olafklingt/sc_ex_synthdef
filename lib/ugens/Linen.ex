defmodule Linen do
  def kr(gate \\ 1.0, attackTime \\ 0.01, susLevel \\ 1.0, releaseTime \\ 1.0, doneAction \\ 0) do
  %UGen.Linen.Kr{gate: gate, attackTime: attackTime, susLevel: susLevel, releaseTime: releaseTime, doneAction: doneAction}
end

end
