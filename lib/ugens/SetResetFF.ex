defmodule SetResetFF do
  def ar(trig \\ 0.0, reset \\ 0.0) do
  %UGen.SetResetFF.Ar{trig: trig, reset: reset}
end
    def kr(trig \\ 0.0, reset \\ 0.0) do
  %UGen.SetResetFF.Kr{trig: trig, reset: reset}
end

end
