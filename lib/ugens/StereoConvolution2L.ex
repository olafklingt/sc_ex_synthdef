defmodule StereoConvolution2L do
  def ar(inv \\ nil, kernelL \\ nil, kernelR \\ nil, trigger \\ 0, framesize \\ 2048, crossfade \\ 1) do
  %StereoConvolution2L.Ar{in: inv, kernelL: kernelL, kernelR: kernelR, trigger: trigger, framesize: framesize, crossfade: crossfade}
end

end
