defmodule DelTapRd do
  def ar(buffer \\ nil, phase \\ nil, delTime \\ nil, interp \\ 1) do
  %DelTapRd.Ar{buffer: buffer, phase: phase, delTime: delTime, interp: interp}
end
    def kr(buffer \\ nil, phase \\ nil, delTime \\ nil, interp \\ 1) do
  %DelTapRd.Kr{buffer: buffer, phase: phase, delTime: delTime, interp: interp}
end

end
