defmodule DetectIndex do
  def ar(bufnum \\ nil, inv \\ 0.0) do
  %DetectIndex.Ar{bufnum: bufnum, in: inv}
end
    def kr(bufnum \\ nil, inv \\ 0.0) do
  %DetectIndex.Kr{bufnum: bufnum, in: inv}
end

end
