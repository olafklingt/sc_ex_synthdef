defmodule RunningSum do
  def ar(inv \\ nil, numsamp \\ 40) do
  %RunningSum.Ar{in: inv, numsamp: numsamp}
end
    def kr(inv \\ nil, numsamp \\ 40) do
  %RunningSum.Kr{in: inv, numsamp: numsamp}
end

end
