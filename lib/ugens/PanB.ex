defmodule PanB do
  def ar(inv \\ nil, azimuth \\ 0, elevation \\ 0, gain \\ 1) do
  %PanB.Ar{in: inv, azimuth: azimuth, elevation: elevation, gain: gain}
end
    def kr(inv \\ nil, azimuth \\ 0, elevation \\ 0, gain \\ 1) do
  %PanB.Kr{in: inv, azimuth: azimuth, elevation: elevation, gain: gain}
end

end
