defmodule MoogFF do
  def ar(inv \\ nil, freq \\ 100, gain \\ 2, reset \\ 0) do
  %MoogFF.Ar{in: inv, freq: freq, gain: gain, reset: reset}
end
    def kr(inv \\ nil, freq \\ 100, gain \\ 2, reset \\ 0) do
  %MoogFF.Kr{in: inv, freq: freq, gain: gain, reset: reset}
end

end
