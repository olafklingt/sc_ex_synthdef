defmodule PV_RandComb do
  def new(buffer \\ nil, wipe \\ 0.0, trig \\ 0.0) do
  %UGen.PV_RandComb.New{buffer: buffer, wipe: wipe, trig: trig}
end

  def description do
    "Pass random bins."
  end

end
