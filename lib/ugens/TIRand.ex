defmodule TIRand do
  def ar(lo \\ 0, hi \\ 127, trig \\ 0.0) do
  %TIRand.Ar{lo: lo, hi: hi, trig: trig}
end
    def kr(lo \\ 0, hi \\ 127, trig \\ 0.0) do
  %TIRand.Kr{lo: lo, hi: hi, trig: trig}
end

end
