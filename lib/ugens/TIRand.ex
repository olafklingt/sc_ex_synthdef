defmodule TIRand do
  def ar(lo \\ 0, hi \\ 127, trig \\ 0.0) do
  %UGen.TIRand.Ar{lo: lo, hi: hi, trig: trig}
end
    def kr(lo \\ 0, hi \\ 127, trig \\ 0.0) do
  %UGen.TIRand.Kr{lo: lo, hi: hi, trig: trig}
end

end
