defmodule GrainFM do
  def ar(numChannels \\ 1, trigger \\ 0, dur \\ 1, carfreq \\ 440, modfreq \\ 200, index \\ 1, pan \\ 0, envbufnum \\ -1, maxGrains \\ 512) do
  %GrainFM.Ar{numChannels: numChannels, trigger: trigger, dur: dur, carfreq: carfreq, modfreq: modfreq, index: index, pan: pan, envbufnum: envbufnum, maxGrains: maxGrains}
end

end
