defmodule Dgeom do
  def new(start \\ 1, grow \\ 2, length \\ nil) do
  %UGen.Dgeom.New{start: start, grow: grow, length: length}
end

  def description do
    "Demand rate geometric series UGen."
  end

end
