defmodule Demand do
  def ar(trig \\ nil, reset \\ nil, demandUGens \\ nil) do
  %Demand.Ar{trig: trig, reset: reset, demandUGens: demandUGens}
end
    def kr(trig \\ nil, reset \\ nil, demandUGens \\ nil) do
  %Demand.Kr{trig: trig, reset: reset, demandUGens: demandUGens}
end

end
