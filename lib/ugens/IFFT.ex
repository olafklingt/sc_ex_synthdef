defmodule IFFT do
  def ar(buffer \\ nil, wintype \\ 0, winsize \\ 0) do
  %IFFT.Ar{buffer: buffer, wintype: wintype, winsize: winsize}
end
    def kr(buffer \\ nil, wintype \\ 0, winsize \\ 0) do
  %IFFT.Kr{buffer: buffer, wintype: wintype, winsize: winsize}
end

end
