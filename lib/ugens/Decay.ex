defmodule Decay do
  def ar(inv \\ 0.0, decayTime \\ 1.0) do
  %Decay.Ar{in: inv, decayTime: decayTime}
end
    def kr(inv \\ 0.0, decayTime \\ 1.0) do
  %Decay.Kr{in: inv, decayTime: decayTime}
end

end
