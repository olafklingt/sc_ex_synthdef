defmodule IEnvGen do
  def ar(envelope \\ nil, index \\ nil) do
  %IEnvGen.Ar{envelope: envelope, index: index}
end
    def kr(envelope \\ nil, index \\ nil) do
  %IEnvGen.Kr{envelope: envelope, index: index}
end

end
