defmodule DelayN do
  def ar(inv \\ 0.0, maxdelaytime \\ 0.2, delaytime \\ 0.2) do
  %UGen.DelayN.Ar{in: inv, maxdelaytime: maxdelaytime, delaytime: delaytime}
end
    def kr(inv \\ 0.0, maxdelaytime \\ 0.2, delaytime \\ 0.2) do
  %UGen.DelayN.Kr{in: inv, maxdelaytime: maxdelaytime, delaytime: delaytime}
end

end
