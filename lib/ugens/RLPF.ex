defmodule RLPF do
  def ar(inv \\ 0.0, freq \\ 440.0, rq \\ 1.0) do
  %UGen.RLPF.Ar{in: inv, freq: freq, rq: rq}
end
    def kr(inv \\ 0.0, freq \\ 440.0, rq \\ 1.0) do
  %UGen.RLPF.Kr{in: inv, freq: freq, rq: rq}
end

end
