defmodule PulseCount do
  def ar(trig \\ 0.0, reset \\ 0.0) do
  %UGen.PulseCount.Ar{trig: trig, reset: reset}
end
    def kr(trig \\ 0.0, reset \\ 0.0) do
  %UGen.PulseCount.Kr{trig: trig, reset: reset}
end

end
