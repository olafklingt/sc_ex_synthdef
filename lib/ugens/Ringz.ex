defmodule Ringz do
  def ar(inv \\ 0.0, freq \\ 440.0, decaytime \\ 1.0) do
  %Ringz.Ar{in: inv, freq: freq, decaytime: decaytime}
end
    def kr(inv \\ 0.0, freq \\ 440.0, decaytime \\ 1.0) do
  %Ringz.Kr{in: inv, freq: freq, decaytime: decaytime}
end

end
