defmodule Dbufrd do
  def new(bufnum \\ 0, phase \\ 0.0, loop \\ 1.0) do
  %UGen.Dbufrd.New{bufnum: bufnum, phase: phase, loop: loop}
end

  def description do
    "Buffer read demand ugen"
  end

end
