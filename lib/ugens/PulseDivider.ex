defmodule PulseDivider do
  def ar(trig \\ 0.0, div \\ 2.0, start \\ 0.0) do
  %PulseDivider.Ar{trig: trig, div: div, start: start}
end
    def kr(trig \\ 0.0, div \\ 2.0, start \\ 0.0) do
  %PulseDivider.Kr{trig: trig, div: div, start: start}
end

end
