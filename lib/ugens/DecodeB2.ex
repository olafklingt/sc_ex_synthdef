defmodule DecodeB2 do
  def ar(numChans \\ nil, w \\ nil, x \\ nil, y \\ nil, orientation \\ 0.5) do
  %DecodeB2.Ar{numChans: numChans, w: w, x: x, y: y, orientation: orientation}
end
    def kr(numChans \\ nil, w \\ nil, x \\ nil, y \\ nil, orientation \\ 0.5) do
  %DecodeB2.Kr{numChans: numChans, w: w, x: x, y: y, orientation: orientation}
end

end
