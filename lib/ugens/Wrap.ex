defmodule Wrap do
  def ar(inv \\ 0.0, lo \\ 0.0, hi \\ 1.0) do
  %UGen.Wrap.Ar{in: inv, lo: lo, hi: hi}
end
    def ir(inv \\ 0.0, lo \\ 0.0, hi \\ 1.0) do
  %UGen.Wrap.Ir{in: inv, lo: lo, hi: hi}
end
    def kr(inv \\ 0.0, lo \\ 0.0, hi \\ 1.0) do
  %UGen.Wrap.Kr{in: inv, lo: lo, hi: hi}
end

end
