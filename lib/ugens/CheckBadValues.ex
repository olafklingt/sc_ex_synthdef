defmodule CheckBadValues do
  def ar(inv \\ 0.0, id \\ 0, post \\ 2) do
  %CheckBadValues.Ar{in: inv, id: id, post: post}
end
    def kr(inv \\ 0.0, id \\ 0, post \\ 2) do
  %CheckBadValues.Kr{in: inv, id: id, post: post}
end

end
