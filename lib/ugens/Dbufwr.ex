defmodule Dbufwr do
  def new(input \\ 0.0, bufnum \\ 0, phase \\ 0.0, loop \\ 1.0) do
  %UGen.Dbufwr.New{input: input, bufnum: bufnum, phase: phase, loop: loop}
end

  def description do
    "Buffer write demand ugen"
  end

end
