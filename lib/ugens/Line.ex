defmodule Line do
  def ar(start \\ 0.0, endv \\ 1.0, dur \\ 1.0, doneAction \\ 0) do
  %UGen.Line.Ar{start: start, end: endv, dur: dur, doneAction: doneAction}
end
    def kr(start \\ 0.0, endv \\ 1.0, dur \\ 1.0, doneAction \\ 0) do
  %UGen.Line.Kr{start: start, end: endv, dur: dur, doneAction: doneAction}
end

end
