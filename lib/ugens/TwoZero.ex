defmodule TwoZero do
  def ar(inv \\ 0.0, freq \\ 440.0, radius \\ 0.8) do
  %UGen.TwoZero.Ar{in: inv, freq: freq, radius: radius}
end
    def kr(inv \\ 0.0, freq \\ 440.0, radius \\ 0.8) do
  %UGen.TwoZero.Kr{in: inv, freq: freq, radius: radius}
end

end
