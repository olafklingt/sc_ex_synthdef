defmodule HenonN do
  def ar(freq \\ 22050, a \\ 1.4, b \\ 0.3, x0 \\ 0, x1 \\ 0) do
  %UGen.HenonN.Ar{freq: freq, a: a, b: b, x0: x0, x1: x1}
end

  def description do
    "Henon map chaotic generator"
  end

end
