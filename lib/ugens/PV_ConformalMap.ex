defmodule PV_ConformalMap do
  def new(buffer \\ nil, areal \\ 0.0, aimag \\ 0.0) do
  %UGen.PV_ConformalMap.New{buffer: buffer, areal: areal, aimag: aimag}
end

  def description do
    "Complex plane attack."
  end

end
