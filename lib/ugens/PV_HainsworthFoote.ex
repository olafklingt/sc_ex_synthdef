defmodule PV_HainsworthFoote do
  def ar(buffer \\ nil, proph \\ 0.0, propf \\ 0.0, threshold \\ 1.0, waittime \\ 0.04) do
  %UGen.PV_HainsworthFoote.Ar{buffer: buffer, proph: proph, propf: propf, threshold: threshold, waittime: waittime}
end

end
