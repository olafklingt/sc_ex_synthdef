defmodule ReplaceOut do
  def ar(bus \\ nil, channelsArray \\ nil) do
  %ReplaceOut.Ar{bus: bus, channelsArray: channelsArray}
end
    def kr(bus \\ nil, channelsArray \\ nil) do
  %ReplaceOut.Kr{bus: bus, channelsArray: channelsArray}
end

end
