defmodule Spring do
  def ar(inv \\ 0.0, spring \\ 1, damp \\ 0) do
  %Spring.Ar{in: inv, spring: spring, damp: damp}
end
    def kr(inv \\ 0.0, spring \\ 1, damp \\ 0) do
  %Spring.Kr{in: inv, spring: spring, damp: damp}
end

end
