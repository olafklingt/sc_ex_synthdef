defmodule TBall do
  def ar(inv \\ 0.0, g \\ 10, damp \\ 0, friction \\ 0.01) do
  %UGen.TBall.Ar{in: inv, g: g, damp: damp, friction: friction}
end
    def kr(inv \\ 0.0, g \\ 10, damp \\ 0, friction \\ 0.01) do
  %UGen.TBall.Kr{in: inv, g: g, damp: damp, friction: friction}
end

end
