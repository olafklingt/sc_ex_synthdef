defmodule TWindex do
  def ar(inv \\ nil, array \\ nil, normalize \\ 0) do
  %UGen.TWindex.Ar{in: inv, array: array, normalize: normalize}
end
    def kr(inv \\ nil, array \\ nil, normalize \\ 0) do
  %UGen.TWindex.Kr{in: inv, array: array, normalize: normalize}
end

end
