defmodule Pluck do
  def ar(inv \\ 0.0, trig \\ 1.0, maxdelaytime \\ 0.2, delaytime \\ 0.2, decaytime \\ 1.0, coef \\ 0.5) do
  %UGen.Pluck.Ar{in: inv, trig: trig, maxdelaytime: maxdelaytime, delaytime: delaytime, decaytime: decaytime, coef: coef}
end

end
