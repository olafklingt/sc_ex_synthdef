defmodule IndexInBetween do
  def ar(bufnum \\ nil, inv \\ 0.0) do
  %IndexInBetween.Ar{bufnum: bufnum, in: inv}
end
    def kr(bufnum \\ nil, inv \\ 0.0) do
  %IndexInBetween.Kr{bufnum: bufnum, in: inv}
end

end
