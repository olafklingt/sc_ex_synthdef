defmodule MouseY do
  def kr(minval \\ 0, maxval \\ 1, warp \\ 0, lag \\ 0.2) do
  %UGen.MouseY.Kr{minval: minval, maxval: maxval, warp: warp, lag: lag}
end

  def description do
    "Cursor tracking UGen."
  end

end
