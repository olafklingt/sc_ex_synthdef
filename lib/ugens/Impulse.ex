defmodule Impulse do
  def ar(freq \\ 440.0, phase \\ 0.0) do
  %UGen.Impulse.Ar{freq: freq, phase: phase}
end
    def kr(freq \\ 440.0, phase \\ 0.0) do
  %UGen.Impulse.Kr{freq: freq, phase: phase}
end

end
