defmodule QuadC do
  def ar(freq \\ 22050, a \\ 1, b \\ -1, c \\ -0.75, xi \\ 0) do
  %UGen.QuadC.Ar{freq: freq, a: a, b: b, c: c, xi: xi}
end

  def description do
    "General quadratic map chaotic generator"
  end

end
